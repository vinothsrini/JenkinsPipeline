pipeline {
    agent any

    stages {
        stage ('Compile Stage') {

            steps {
                def mvnTool = tool 'maven_3.8_3'

				// execute maven
				sh "${mvnTool}/bin/mvn clean" 
            }
        }
}
}